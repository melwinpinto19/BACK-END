// importing the data from another file
let data = require("./script");

// node js (it is a combination of v8 engine(c++) and js wrapper which allows us to create js runtime environments)
// v8 engine is inbuilt in most of the browsers because of it we can run js on the browser
// in the core js is coverted to the c++ and executed

/*
https://google.com/feed/post
here '/feed', '/post' is an route

GET route (the data is visible in the url) eg:search in google
POST route(the data is not visible in the url) eg:form submission
*/

console.log(data);
